server:
  port: 8002

spring:
  application:
    name: auth
  config:
    import: optional:file:.env[.properties]
  security:
    user:
      name: admin
      password: admin

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL}